/// [Themes] enum is used to define the themes of the application.
enum Themes {
  light("light"),
  dark("dark");

  /// [label] is the name of the theme.
  final String label;

  const Themes(this.label);
}
